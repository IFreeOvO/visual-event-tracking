export enum WS_FROM {
    DEVTOOL = '1',
    CLIENT = '2',
}

export enum SDKEvent {
    'EnableInspect' = 'enableInspect',
    'DisableInspect' = 'disableInspect',
}

export enum EventType {
    'SDK' = 'SDK',
    'CDP' = 'CDP',
}
