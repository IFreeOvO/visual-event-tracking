export enum EventTypeEnum {
    Click,
    Expose,
}

export enum SiblingEffectiveEnum {
    No,
    YES,
}

export enum SendTypeEnum {
    Beacon = 'beacon',
    Gif = 'gif',
}

export enum RuleRequiredEnum {
    NO,
    YES,
}

export enum ModeEnum {
    Validate = 'validate',
    Report = 'report',
    ReportAndValidate = 'reportAndValidate',
}

export enum ValidateStatusEnum {
    Fail,
    Success,
}
